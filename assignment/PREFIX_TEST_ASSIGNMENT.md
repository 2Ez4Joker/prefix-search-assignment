# Тестовое задание: ранжирование префиксного поиска для «грузового» каталога

> 📌 **Важно.** Перед стартом прочитайте `docs/CANDIDATE_DELIVERY_GUIDE.md` — там описаны требования к Docker, README и процесс сдачи.

## 1. Описание сценария
Вам нужно реализовать поиск по каталогу из ~1000 товаров (XML `data/catalog_products.xml`) с упором на префиксные запросы. Пользователи вводят запросы с мобильных устройств, совершают опечатки, используют латиницу и смешанные языки, добавляют числовые признаки (объём, вес, размер). От качества ранжирования зависит, увидит ли пользователь релевантный товар за первые 1–2 секунды.

### Персоны
- **Mobile Shopper (Store D)** – печатает «ма», «масло сл…», ожидает постепенное уточнение без «прыжков» между категориями.
- **Premium Buyer (Store F)** – вводит латинские названия вин “prosecco rose”, “riesling mosel”.
- **B2B Buyer (Store C)** – ищет пачки товара с указанием веса/литража `масло раст 10л`, `cheddar 5kg`.
- **General Retail (Store E)** – часто ошибается с раскладкой (`xfq` вместо «чай»), но хочет видеть товары в правильной категории.

## 2. Исходные данные
1. **Каталог**: `data/catalog_products.xml` (1000 SKU). В каждом товаре есть поля: id, name, category, brand, weight, package_size, keywords, description, price, image_url. URL и цены вымышленные.
2. **Список запросов**: `data/prefix_queries.csv` (30 «открытых» + 30 «скрытых» запросов). В таблице указано, для какого условного магазина (Store A…F) предназначен запрос и какие особенности стоит учитывать.
3. **Дополнительно** (опционально): JSON с префиксными подсказками или map для трансформации раскладки.

## 3. Требования к решению
### 3.1 Инфраструктура и данные
- Можно использовать любой движок: Elasticsearch/OpenSearch, Milvus/Faiss, гибрид.
- Написать скрипт загрузки XML в выбранный движок. Допустимо конвертировать в JSON/CSV перед загрузкой.
- Сформировать анализаторы/индексы, позволяющие:
  - искать по префиксам (edge n-gram, match_bool_prefix и т.п.);
  - учитывать транслитерации/раскладку;
  - хранить числовые атрибуты (вес, объём) для фильтрации/бустов.

### 3.2 Ранжирование и обработка запросов
- Обязательные особенности:
  - поддержка коротких префиксов (1–3 буквы) без агрессивного переписывания запроса;
  - сохранение числовых признаков (`10л`, `3 кг`) и учёт их в выдаче;
  - защита от «мусорных» результатов (возврат только релевантных категорий/атрибутов);
  - обработка смешанной раскладки и транслитерации;
  - логирование оригинального запроса, применённой нормализации и итогового поиска.
- Желаемые расширения (по выбору):
  - векторный/семантический скоринг для длинных запросов;
  - собственный «prefix purity score»/поведенческие бусты;
  - механизм подсказок (prefix map) с fallback’ом.

### 3.3 Оценка качества
- Реализовать вспомогательный скрипт `evaluate.py` (или notebook), который:
  - прогоняет открытый набор запросов;
  - сохраняет Top-5 результатов с пояснением/шкала (score, категория, почему релевантен);
  - считает хотя бы один метрик (например, precision@3, доля попаданий нужной категории, latency).
- Описать в README найденные проблемы и предложения по улучшению (раздел «Reflection»).

### 3.4 Документация и системное мышление
- README должен содержать:
  - инструкции по запуску (docker-compose / команды);
  - описание схемы индекса и логики ранжирования;
  - список сценариев из `prefix_queries.csv`, на которые вы ориентировались;
  - описание, как вы снижали количество «мусора» в выдаче;
  - раздел «Если бы это шло в прод», где вы перечисляете мониторинги/алерты и дальнейшие улучшения.
- Приветствуется ссылка на внешние исследования/статьи, которыми вы вдохновлялись (IR, spellchecking, LLM).

## 4. Критерии оценки (10-балльная шкала)
| Пункт | Вес | Что оцениваем |
| --- | --- | --- |
| Загрузка и модель данных | 2 | Корректный импорт, осмысленные поля, подготовка данных. |
| Обработка префиксов и ранжирование | 3 | Релевантность Top-N, работа с опечатками, минимизация шума. |
| Метрики и тесты | 2 | Скрипты проверки, результаты экспериментов, интерпретация. |
| Документация и системное мышление | 2 | Ясный README, архитектура, идеи для прод-эксплуатации. |
| Инновации/Stretch | 1 | Вектора, LLM, дополнительные инструменты, creative подход. |

**Максимум: 10 баллов.**  
Порог «проходного» решения – 6+. Баллы могут снижаться за отсутствие документации или игнорирование ключевых сценариев.

## 5. Что сдавать
- Код и конфигурации (репозиторий/архив).
- Каталоги/скрипты: `load_catalog.py`, `evaluate.py` или аналогичные.
- README (на русском или английском).
- Логи/скриншоты запросов (можно приложить CSV с итогами).

Удачи! вопросительные моменты можно обсудить с интервьюером (но не обязательно ждать ответ). Старайтесь показать ваше системное мышление и умение принимать решения на основе данных.
